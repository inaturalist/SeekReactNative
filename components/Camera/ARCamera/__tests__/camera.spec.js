// @flow

// import * as React from "react";
// import { fireEvent, render, waitFor } from "test-utils";

// import ARCameraOverlay from "../ARCameraOverlay";

// describe( "takePicture", () => {
//   it( "button should not be disabled on initial render", async () => {
//     const navigation = { navigate: jest.fn() };

//     const { getByTestId, queryByTestId } = render(
//       <ARCameraOverlay
//         ranks={{
//           "species": [{
//             "ancestor_ids": [],
//             "name": "Ananas comosus",
//             "rank": 10,
//             "score": 0.84,
//             "taxon_id": 122965}
//           ]}}
//           navigation={navigation}
//         /> );
//     const cameraButton = getByTestId( "takePhotoButton" );

//     expect( cameraButton ).not.toBeDisabled( );
//   } );

//   it( "tries to save a photo to the user's camera roll", async () => {

//   } );

//   it( "shows an error alert if the photo cannot be saved", async () => {

//   } );
// } );
