import { StyleSheet } from "react-native";
import {
  fonts
} from "../global";

export default StyleSheet.create( {
  circleStyle: {
    width: 59,
    height: 59
  },
  circleText: {
    fontFamily: fonts.book,
    fontSize: 20
  }
} );
