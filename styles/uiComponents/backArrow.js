import { StyleSheet } from "react-native";
import { touchable } from "../global";

export default StyleSheet.create( {
  backButton: {
    left: 23,
    paddingBottom: 18,
    paddingTop: 18
  },
  touchable
} );
